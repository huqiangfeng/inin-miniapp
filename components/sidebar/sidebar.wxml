<!-- components/sidebar/sidebar.wxml -->
<scroll-view scroll-y="true" class="scroll">
  <view class="scroll-item {{index === activeKey?'active':'' }}" bind:tap="change" data-index="{{index}}" wx:for="{{list}}" wx:key="index">
    {{ item.name }}
  </view>
</scroll-view>